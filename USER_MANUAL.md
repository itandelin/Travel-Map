# WordPress Travel Map 用户手册

适用版本：1.1.0

本手册描述插件当前实际具备的功能。文档末尾的「已知限制」列出了尚未实现或存在缺陷的部分，请一并阅读。

## 目录

1. [插件简介](#插件简介)
2. [快速开始](#快速开始)
3. [设置页面](#设置页面)
4. [坐标管理](#坐标管理)
5. [导入导出](#导入导出)
6. [短代码使用](#短代码使用)
7. [文章关联](#文章关联)
8. [前台交互说明](#前台交互说明)
9. [权限说明](#权限说明)
10. [常见问题](#常见问题)
11. [已知限制](#已知限制)

## 插件简介

Travel Map 把你的旅行地点标注在一张基于高德地图的交互地图上，分为三种状态：

- **已去（`done`）**：去过的地方，可以关联旅行文章。如果关联文章有特色图，地图标记会直接显示这张图。
- **想去（`wish`）**：愿望清单，可以写想去的理由。
- **计划（`plan`）**：已排期的行程，可以填计划日期。

旧值 `visited`/`want_to_go`/`planned` 仍被接受并自动映射为上面的新值。

前台通过短代码 `[travel_map]` 输出地图，访客可以按状态筛选，点击标记查看详情或跳转文章。

插件自带版本化的数据库迁移：激活或升级后会自动比对数据库版本（设置项 `travel_map_db_version`），按需迁移状态取值、补齐新增字段并清理废弃配置；卸载时会删除两张数据表和插件写入的全部设置项。

## 快速开始

### 第一步：申请高德地图密钥

1. 访问[高德开放平台](https://lbs.amap.com/)并注册登录。
2. 创建应用，添加 Key，服务平台选择 **Web 端（JS API）**。
3. 复制 **Key**，同时在同一页面获取 **安全密钥（securityJsCode）**。

2021 年 12 月 2 日之后申请的 Key 必须搭配安全密钥使用，两者都要填。

由于密钥会输出到前台页面源码中（高德 JS API 的固有工作方式），请务必在高德控制台为该 Key 绑定域名白名单，否则他人可以盗用你的配额。

### 第二步：配置插件

进入后台 **Travel Map → 地图设置**，填入 API 密钥和安全密钥，点击「保存设置」。

### 第三步：添加地点

进入 **Travel Map → 坐标管理**，在右侧地图上点击选择位置，填写地点名称，选择状态，点击「添加坐标」。

### 第四步：显示地图

在任意文章或页面内容中插入短代码：

```
[travel_map]
```

## 设置页面

**Travel Map → 地图设置**（需要 `manage_options` 权限，即管理员）。

### API 配置

| 字段 | 说明 |
|------|------|
| 高德地图 API 密钥 | 必填。Web 端（JS API）的 Key |
| 高德地图安全密钥 | 必填。与 Key 配对的 securityJsCode |

### 地图显示配置

| 字段 | 默认值 | 说明 |
|------|--------|------|
| 默认缩放级别 | 4 | 下拉选择 1–18。1–4 世界地图，5–8 国家级，9–12 城市级，13+ 街道级 |
| 默认地图中心点 | `35.0,105.0` | 格式为 **纬度,经度**（注意顺序），默认为中国地理中心 |

### 功能选项

| 字段 | 默认值 | 说明 |
|------|--------|------|
| 显示筛选标签栏 | 开启 | 在地图上方显示「全部/已去/想去/计划」筛选标签（数量为 0 的状态自动隐藏） |
| 自适应缩放 | 开启 | 开启后地图加载与筛选切换时自动调整视野包含全部标记点；关闭则固定使用默认中心与缩放 |
| 高亮已去国家 | 开启 | 把「已去」标记涉及的国家整片填绿（需要标记填写了国别字段） |
| 显示年度统计面板 | 开启 | 地图左下角的年度统计面板 |
| 显示状态统计面板 | 开启 | 地图右下角的状态统计面板 |

### 聚合、视野与其他选项

| 字段 | 默认值 | 说明 |
|------|--------|------|
| 聚合半径 | 40 | 像素。数值越大越容易合并成聚合气泡（20–200） |
| 聚合数字上限 | 9 | 聚合气泡上显示的最大数字，超出仍显示该值（1–999） |
| 缺省筛选状态 | 全部 | 前台地图首次加载时激活的筛选页签。短代码显式写 `status="…"` 时以短代码为准 |
| 最小缩放级别 | 1 | 地图可缩小到的最小级别 |
| 最大缩放级别 | 12 | 地图可放大到的最大级别 |
| 缺省缩略图地址 | 空 | 地点无封面且无关联文章特色图时，标记使用的兜底图片地址 |

### 标记颜色
1.1.0 起设置页已不再提供颜色设置项，标记颜色由状态自动决定，不能手动配置：

| 状态 | 颜色 | 形态 |
|------|------|------|
| 已去 | 有游记时直接显示关联文章的特色图；无游记时为蓝灰渐变 `#89a9cd` → `#485f81` | 照片（圆角方形）/ 圆点 |
| 想去 | 黄橙渐变 `#f9c74f` → `#f9844a` | 12px 圆点 |
| 计划 | 紫色渐变 `#9b59b6` → `#8e44ad` | 12px 圆点 |

地图上方筛选标签的颜色是另一套（全部红、已去橙、想去琥珀、计划绿），与标记配色不是同一体系。

设置页下方还有「使用说明」区块，内置短代码参数表和可一键复制的示例代码。

## 坐标管理

**Travel Map → 坐标管理**（需要 `edit_posts` 权限，即作者及以上）。

页面分为左右两栏：左侧是地点列表，右侧是地图选点和编辑表单。

### 地点列表

- **筛选状态**：下拉选择全部/已去/想去/计划。
- **搜索**：输入关键词，匹配地点名称、描述以及关联文章标题。
- **表格列**：勾选框、地点名称、状态、坐标、关联文章、创建时间、操作。
- **编辑**：点击行末「编辑」按钮，数据会载入右侧表单，页面自动滚动到表单位置。
- **批量删除**：勾选若干行后，「批量删除」按钮出现，确认后删除。需要 `delete_posts` 权限。

列表通过 AJAX 一次性加载全部数据，筛选和搜索都会重新请求。

### 添加与编辑表单

**选择坐标位置**：在右侧地图上点击任意位置，或拖动地图上的标记，经纬度会自动填入表单。也可以直接在经纬度输入框中键入数值，地图标记会跟随移动。

**基本信息**

| 字段 | 必填 | 说明 |
|------|------|------|
| 地点名称 | 是 | 例如「北京天安门广场」 |
| 纬度 | 是 | -90 到 90 |
| 经度 | 是 | -180 到 180 |
| 旅行状态 | 是 | 已去 / 想去 / 计划 |
| 标记颜色 | 否 | 可填写并会保存，但前台不参与渲染（前台按状态统一取色） |

**关联与日期**（随状态切换显示）

| 字段 | 出现条件 | 说明 |
|------|----------|------|
| 关联文章 | 始终 | 下拉选择一篇已发布文章作为主关联 |
| 访问日期 | 状态为「已去」 | 日期选择器 |
| 计划日期 | 状态为「计划」 | 日期选择器 |
| 想去理由 | 状态为「想去」 | 多行文本 |
| 地点描述 | 始终 | 多行文本 |

选择状态时，对应的日期/理由字段会自动显示或隐藏。

**按钮**：「添加坐标」/「更新坐标」提交表单；「重置」清空表单并回到新增模式。

地点名称使用清晰完整的写法（如「法国巴黎埃菲尔铁塔」）有助于后续搜索和文章匹配。

## 导入导出

### 导出

点击列表上方「导出」按钮，浏览器会下载一个 CSV 文件，文件名形如 `travel-map-export-2026-02-14.csv`，内容为全部地点。界面只提供 CSV 按钮（`assets/js/travel-map-coordinates.js:510-518` 写死 `format: 'csv'`）；后端 `ajax_export()`（`travel-map.php:1329`）通过 `format` 参数同样支持 JSON，`export_json()`（`travel-map.php:2007`）输出 `.json` 文件，但需要手动构造请求，**没有界面入口**。

CSV 列顺序：

```
ID, Title, Latitude, Longitude, Status, Description,
Visit Date, Visit Count, Planned Date, Wish Reason,
Priority Level, Country, Years, Cover Image, Type, Created At
```

### 导入

点击「导入」按钮打开弹窗，选择文件后提交。支持两种格式：

**CSV**：需与导出格式的列顺序一致。首行作为表头被跳过，从第二行开始读取。第 2 列起依次为标题、纬度、经度、状态……状态值应使用 `done`、`wish`、`plan`（旧值 `visited`/`want_to_go`/`planned` 仍被接受并自动映射），无法识别的值会回退为 `done`。

**JSON**：一个对象数组，每个对象至少包含 `title`、`latitude`、`longitude`：

```json
[
  {
    "title": "北京天安门广场",
    "latitude": 39.908822,
    "longitude": 116.397496,
    "status": "done",
    "description": "第一次去北京",
    "visit_date": "2024-05-01",
    "visit_count": 1
  }
]
```

导入采用纯追加方式：不做去重，不更新已有记录，每一行都会新建一个地点。重复导入同一个文件会产生重复数据。

导入前建议先导出一份备份。

## 短代码使用

### 基础用法

```
[travel_map]
```

### 参数

插件共支持 8 个参数：

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `width` | `100%` | 地图宽度，任意 CSS 长度值 |
| `height` | `550px` | 地图高度，任意 CSS 长度值 |
| `zoom` | 设置页的默认缩放级别 | 缩放级别 1–18 |
| `center` | 设置页的默认中心点 | 格式 `纬度,经度` |
| `filter_tabs` | 设置页的开关值 | `true` / `false`，是否显示筛选标签 |
| `status` | 空 | `all` / `done` / `wish` / `plan`，初始显示的状态 |
| `filters` | `all,done,wish,plan` | 地图内筛选标签包含哪些状态，逗号分隔，可裁剪 |
| `auto_zoom` | 空 | 空表示跟随设置页的「自适应缩放」，也可写 `true` / `false` 单独覆盖 |

`status` 默认留空是有意为之：不写时使用设置页的「缺省筛选状态」（默认「全部」），这样才能与显式写 `status="all"` 区分开。`status` 只接受单个值，不支持用逗号列举多个状态。

### 示例

大尺寸地图：

```
[travel_map height="700px" zoom="3"]
```

只显示已去的地点并隐藏筛选标签：

```
[travel_map status="done" filter_tabs="false"]
```

聚焦到某个区域：

```
[travel_map center="39.9,116.4" zoom="6" height="600px"]
```

### 另外两个短代码

**`[travel_map_countries]`**：不带任何参数。按「已去」状态地点的国别字段分组（一个地点可以填多个国家，用逗号分隔），输出国旗、国家代码与该国的地点数量，按地点数量由多到少排列。

**`[travel_map_markers]`**：以列表形式输出地点（国旗、地名、坐标、年份、游记封面网格），不显示地图。

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `status` | `done` | `done` / `wish` / `plan` / `all`，要列出的状态 |
| `type` | 空 | 按地点的分类字段过滤，留空表示不限 |

### 在主题模板或小工具中使用

短代码写在文章或页面正文里时，插件会自动加载所需的 CSS 和 JS。如果你把短代码放在主题模板文件或小工具中，建议先显式加载资源：

```php
<?php
if (function_exists('travel_map_enqueue_assets')) {
    travel_map_enqueue_assets();
}
?>
<?php echo do_shortcode('[travel_map]'); ?>
```

## 文章关联

一个地点可以关联多篇文章，一篇文章也可以关联多个地点。

### 在文章编辑页关联

编辑文章时，内容编辑器下方有「旅行地点坐标」Meta Box（默认对文章和页面启用），包含两部分：

**选择已有地点标记**

- 顶部统计条显示总数、已选数量以及三种状态各自的数量。
- 搜索框按地点名称过滤，按 Esc 清空。
- 「全部/已去/想去/计划」按钮按状态过滤。
- 勾选地点即建立关联，点击整个卡片也可切换勾选状态。

保存文章时，勾选结果会覆盖该文章此前的全部关联关系。列表中的第一个地点会被记为该文章的「主关联」。

**或创建新地点标记**

填写地点名称，在小地图上点击选择位置（也可点「手动输入坐标」切换为直接键入），选择状态并填写描述，保存文章时会新建这个地点并自动关联到当前文章。

### 在坐标管理页关联

编辑地点时，通过「关联文章」下拉框选择一篇文章。这个字段设置的是地点的主关联文章。

### 关联效果

前台点击「已去」状态的标记时，弹窗会列出该地点关联的所有已发布文章（标题、按创建顺序排列），点击标题在新标签页打开文章。弹窗顶部显示第一篇文章的特色图。

地图标记本身也会优先使用关联文章的特色图显示（圆角方形缩略图），找不到特色图时回退为状态配色的圆点。

## 前台交互说明

**筛选标签**：点击「全部/已去/想去/计划」重新加载对应状态的标记。

**地图控制按钮**：右上角四个按钮依次为放大、缩小、全屏、重置视图。

**点击标记**：地图先平滑缩放并将标记移动到视野中心偏下位置，然后弹出信息窗口。「已去」且有关联文章的地点显示文章列表弹窗，其他情况显示简洁弹窗（想去显示想去理由和描述，计划显示计划日期和描述）。

**悬停预览**：桌面端把鼠标移到带图片的「已去」标记上，会浮出一个小预览卡片。

**标记聚合**：缩放级别较小时，彼此靠近的标记会合并成聚合气泡（`AMap.MarkerCluster`），点击气泡地图会自动放大展开。聚合半径与显示的数字上限可在设置页调整。

**统计面板**：地图右下角显示状态统计（已去 / 想去 / 计划 / 总计），左下角显示年度统计（含已去国家数量），两者都会随筛选联动，也可以在设置页分别关闭。

**国家高亮**：开启设置页的「高亮已去国家」后，已去地点涉及的整片国家会在地图上填色（需要地点填写了国别字段）。

**关闭弹窗**：点击关闭按钮、点击弹窗外部、按 Esc 键，移动端还可以向下滑动弹窗。

**深浅色适配**：地图样式会自动跟随站点主题。插件监听 `<html>` 元素上的 `dark`、`light`、`auto` class，以及系统的 `prefers-color-scheme`，切换时地图样式实时更新，无需配置。

**移动端**：屏幕宽度 ≤768px 时地图改为竖版比例（高度约为宽度的 1.3 倍），≤480px 时为 1.5 倍；同时禁用滚轮缩放以免影响页面滚动，初始缩放级别自动降低一级。

**数据加载**：前台优先通过 `admin-ajax.php` 拉取数据。如果服务器或安全插件拦截了该请求（返回 401/403/405），会自动改用 REST API 接口重试，无需手动配置。

## 权限说明

| 操作 | 所需能力 | 典型角色 |
|------|----------|----------|
| 查看前台地图 | 无 | 所有访客 |
| 访问地图设置页 | `manage_options` | 管理员 |
| 访问坐标管理页 | `edit_posts` | 作者及以上 |
| 新增/编辑地点 | `edit_posts` | 作者及以上 |
| 删除地点、批量删除 | `delete_posts` | 作者及以上 |
| 导入数据 | `edit_posts` | 作者及以上 |
| 在文章中关联地点 | `edit_post`（该文章） | 该文章的编辑者 |

插件没有按用户隔离数据：所有具备 `edit_posts` 的用户看到并可编辑同一份地点数据，地点表中不记录创建者。

## 常见问题

**地图不显示，只看到「请先配置高德地图API密钥」**

设置页的 API 密钥为空。填入 Key 并保存。

**地图区域一直显示加载动画**

依次检查：Key 与安全密钥是否都已填写且正确；高德控制台是否已把当前域名加入白名单；Key 的服务平台是否为「Web 端（JS API）」；浏览器控制台是否有网络错误；Key 的日调用量是否已用完。

**地图容器高度为 0 或样式错乱**

通常是主题没有正确调用 `wp_head()` / `wp_footer()`。插件内置了三层兜底机制（内联关键 CSS、短代码渲染时强制输出资源、前端脚本运行时补注入样式），多数情况能自动恢复。如果仍有问题，在模板中调用 `travel_map_enqueue_assets()`。

**改了文章特色图，地图上还是旧图**

标记数据有 5 分钟缓存。通过文章 Meta Box 保存会立即清除缓存；只更换特色图而不改关联关系时，需等待缓存过期。

**修改单个地点的「标记颜色」没有效果**

前台按状态统一取色，单个地点的颜色字段虽然会保存，但不参与渲染；设置页也不再提供颜色设置项。

**能导入 Google Maps 或 GPS 轨迹数据吗**

只支持 CSV 和 JSON 两种格式。KML、GPX、GeoJSON 都需要先自行转换为上述 CSV 列结构或 JSON 结构。

**如何备份数据**

用「导出」按钮下载 CSV，或直接备份数据库中的 `wp_travel_map_markers` 和 `wp_travel_map_post_markers` 两张表（表前缀随站点配置变化）。

**地点很多时地图变慢**

插件已实现标记聚合（`AMap.MarkerCluster`），邻近的标记会合并成气泡，可以调大设置页的「聚合半径」让合并更容易。此外「已去」且有关联文章的地点需要额外查询特色图，数量很大时依然会变慢，可以用 `filters` 参数裁掉不需要的状态页签，或拆成多张地图。

## 已知限制

以下是 1.1.0 仍存在的限制与缺陷，避免误解：

**功能层面**

- **GeoJSON 导入至今不支持**（1.1.0 仍存在）——`templates/coordinates-list.php:349` 的文件选择框 `accept` 里仍写着 `.geojson`，但后端只解析 csv 与 json（`travel-map.php:1364-1370`），选中 GeoJSON 文件会提示「不支持的文件格式」。KML、GPX 同样不支持。
- 没有地址搜索（地理编码）功能，坐标只能通过地图点选或手动输入获得；也不会从文章内容自动识别地名。
- 没有 PDF 报告功能。
- 单个地点的「标记颜色」字段（数据库列 `marker_color`，`travel-map.php:1611`）、`is_featured` 字段（`travel-map.php:1607`）和 `priority_level` 字段目前不影响前台展示。
- 没有按用户隔离数据，也没有单条地点的公开/隐藏开关。

**数据与安全**

- **`ajax_export()` 缺 capability 检查**（1.1.0 仍存在）——`travel-map.php:1330` 只校验 nonce，没有调用 `current_user_can()`。任何已登录用户（包括订阅者）凭有效 nonce 就能导出完整地点数据，如果站点开放注册且地点数据敏感，请留意这一点。
- **`ajax_get_marker()` 缺 capability 检查**（1.1.0 仍存在）——`travel-map.php:1212` 同样只有 nonce 校验，可以逐条读取任意标记的完整记录。
- **导入文件不走上传流程**（1.1.0 仍存在）——`travel-map.php:1359` 直接读取 `$_FILES['import_file']['tmp_name']`，只凭扩展名判断类型，没有调用 `wp_handle_upload()`。
- **导入不校验经纬度范围**（1.1.0 仍存在）——超出 ±90 / ±180 的数据一样会被写入数据库。
- **导入不去重**（1.1.0 仍存在）——重复导入同一份文件会不断新增地点。
- **卸载时未清理 transient**（1.1.0 仍存在）——`uninstall()`（`travel-map.php:1565-1585`）会删除两张数据表和插件写入的全部设置项，但没有 `delete_transient()` 调用，标记数据的缓存项会残留。
- **关联表没有 `UNIQUE(post_id, marker_id)` 约束**（1.1.0 仍存在）——同一篇文章与同一个地点理论上可以重复关联。
- 标记的特色图是逐条查询的，关联文章很多时存在 N+1 查询；缓存失效也没有监听 `save_post` / `updated_post_meta`，只改特色图而不改关联关系时需要等缓存过期。

**界面与代码维护**

- **`travel-map-admin.js` 仍在无条件加载**（1.1.0 仍存在）——`travel-map.php:655-659` 一带的 `wp_enqueue_script('travel-map-admin', ...)` 不在任何页面条件分支内；其中的 `bulkExport()` 提交的 `travel_map_bulk_export` 动作（`assets/js/travel-map-admin.js:500`）后端从未注册。
- **表单提交路径保留但没有界面入口**（1.1.0 仍存在）——`handle_marker_action()` / `handle_add_marker()` / `handle_edit_marker()` / `handle_delete_marker()`（`travel-map.php:2130`、`2153`、`2190`、`2232`）仍随坐标管理页一起加载（`travel-map.php:454-456`），但页面上的表单都是 AJAX 驱动、不带 `action` 属性，因此没有入口能触发它们。
- **后端存在批量修改状态的接口，但界面没有对应入口。**
- **关键样式仍有多份副本**（1.1.0 仍存在）——`travel-map-popup` 相关样式同时出现在 `assets/css/travel-map.css`（29 处）、`assets/js/travel-map.js` 内联（19 处）和 `templates/shortcode-markers.php`（1 处），改动时需要同步。
- **`preventThemeConflicts()` 仍在**（1.1.0 仍存在）——`assets/js/travel-map.js`（定义在 `:1266`，调用点 `:165`）在捕获阶段对地图容器内的 `img` 调用 `stopImmediatePropagation`（4 处：`:1284`、`:1291`、`:1301`、`:1313`），可能连带屏蔽主题自己的图片事件。
- **没有自动化测试**，也缺少 WordPress 插件目录要求的 `readme.txt` 与 `LICENSE`（仓库里只有 `README.md`）。

**国际化**

- **文本域使用常量而非字面量**（1.1.0 仍存在）——代码中用的是 PHP 常量而不是字面量 `'travel-map'`，标准 i18n 提取工具无法自动扫描出待翻译字符串。`languages/travel-map.pot` 已于 1.1.0 重新生成（735 行，`Project-Id-Version: Travel Map 1.1.0`，`languages/travel-map.pot:9`），但受上述限制仍可能有遗漏。界面文案以简体中文为主。

---

**手册版本**：1.1.0  
**对应插件版本**：1.1.0  
**维护**：Mr. T
