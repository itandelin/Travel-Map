# WordPress Travel Map 安装指南

适用版本：1.1.0

## 目录

1. [环境要求](#环境要求)
2. [申请高德地图密钥](#申请高德地图密钥)
3. [安装插件](#安装插件)
4. [配置插件](#配置插件)
5. [添加第一个地点](#添加第一个地点)
6. [在页面中显示地图](#在页面中显示地图)
7. [故障排除](#故障排除)
8. [卸载](#卸载)

## 环境要求

| 项目 | 要求 |
|------|------|
| WordPress | 5.0 或更高 |
| PHP | 7.4 或更高 |
| MySQL | 5.7 或更高 |
| 外部依赖 | 高德地图 Web 端（JS API）Key + 安全密钥 |
| 网络 | 访客浏览器需能访问 `webapi.amap.com` |

PHP 版本低于 7.4 时，插件会在后台显示警告提示。

插件会在服务器上创建两张数据表（`前缀_travel_map_markers` 和 `前缀_travel_map_post_markers`），因此数据库用户需要有建表权限。

地图渲染完全在访客浏览器端完成，服务器不需要能访问高德接口。

## 申请高德地图密钥

插件需要两个凭证：**Key** 和**安全密钥**。2021 年 12 月 2 日之后申请的 Key 必须配合安全密钥使用，缺一个地图都无法加载。

### 步骤 1：注册开发者账号

1. 访问[高德开放平台](https://lbs.amap.com/)。
2. 注册并登录，选择个人开发者或企业开发者。
3. 完成实名认证（个人开发者也需要）。

### 步骤 2：创建应用并添加 Key

1. 进入控制台 → 应用管理 → 我的应用。
2. 创建新应用，填写应用名称。
3. 在应用下点击「添加 Key」，服务平台务必选择 **Web 端（JS API）**。

选错平台是最常见的失败原因。「Web 服务（REST API）」类型的 Key 不能用于本插件。

### 步骤 3：绑定域名白名单

在添加 Key 时填写你的网站域名。这一步不是可选项：Key 和安全密钥都会输出到前台页面源码中（高德 JS API 的固有工作方式，无法避免），任何访客都能查看。绑定域名白名单后，即使密钥被他人获取也无法在其他站点使用。

### 步骤 4：获取安全密钥

在 Key 的详情页面找到安全密钥（securityJsCode），复制备用。

## 安装插件

### 方法一：后台上传 ZIP

1. 把插件目录打包为 zip 文件。
2. 后台 → 插件 → 安装插件 → 上传插件。
3. 选择 zip 文件，点击「现在安装」。
4. 安装完成后点击「启用插件」。

### 方法二：手动上传

1. 通过 FTP 或 SSH 连接服务器。
2. 把整个 `travel-map` 目录上传到 `wp-content/plugins/`。
3. 后台 → 插件 → 已安装的插件，找到 Travel Map 并点击「启用」。

最终路径应为 `wp-content/plugins/travel-map/travel-map.php`。

插件尚未发布到 WordPress.org 插件目录，无法通过后台搜索安装。

### 验证安装

启用后：

- 后台左侧菜单出现「Travel Map」项（图标为定位标记）。
- 数据库中新增 `travel_map_markers` 和 `travel_map_post_markers` 两张表。

菜单没出现通常是目录层级不对（例如多套了一层文件夹），检查主文件是否位于插件目录的第一层。

## 配置插件

进入 **Travel Map → 地图设置**（需要管理员权限）。

### 必填项

| 字段 | 填什么 |
|------|--------|
| 高德地图 API 密钥 | 上一步申请的 Web 端 JS API Key |
| 高德地图安全密钥 | 与该 Key 配对的 securityJsCode |

### 可选项

| 字段 | 默认值 | 建议 |
|------|--------|------|
| 默认缩放级别 | 4 | 展示跨国足迹用 3–4，单个国家用 5–6 |
| 默认地图中心点 | `35.0,105.0` | 格式为 **纬度,经度**，注意不是经纬度顺序 |
| 显示筛选标签栏 | 开启 | 保持开启，方便访客切换状态 |
| 自适应缩放 | 开启 | 加载与筛选后自动调整视野包含全部标记 |
| 高亮已去国家 | 开启 | 需要地点填写国别 ISO 代码（如 `CN`） |
| 年度/状态统计面板 | 开启 | 叠加在地图左下/右下的半透明面板 |
| 聚合半径 | 40 | 数值越大越容易合并成聚合气泡 |
| 聚合数字上限 | 9 | 聚合气泡显示的最大数字 |
| 缺省筛选状态 | 已去 | 地图首次加载时选中的筛选 |
| 缺省封面图 | 空 | 地点无封面且无关联文章特色图时的兜底图 |
| 最小/最大缩放级别 | 1 / 12 | 限制地图缩放范围 |

> 1.1.0 起，标记颜色由状态自动决定（已去橙红 / 想去黄橙 / 计划紫色），旧版的三色设置已移除。

填完点击「保存设置」。

设置页没有密钥有效性验证按钮，密钥是否可用要到实际加载地图时才能确认。

### 关于「世界地图」权限（可选增强）

高德地图的**境外详细路网和 POI** 属于「世界地图」高级能力：

- 未开通时：地图仍可显示全球轮廓与国家高亮（DistrictLayer.World 为免费标准能力），聚合、弹窗、统计等全部功能正常，仅境外区域放大后细节较粗；
- 需要境外精细底图时：在高德开放平台控制台**提交工单申请开通「世界地图」权限**（个人认证账号可尝试申请，企业认证可申请范围更广）；
- 开通后无需改代码，插件初始化时已带 `showOversea: true`。

## 添加第一个地点

进入 **Travel Map → 坐标管理**（需要 `edit_posts` 权限）。页面左侧是地点列表，右侧是地图和表单。

1. 在右侧地图上点击目标位置，经纬度会自动填入下方表单。也可以拖动地图上的标记微调，或直接在输入框里键入坐标。
2. 填写「地点名称」，例如「北京天安门广场」。
3. 选择「旅行状态」：
   - **已去**：会出现「访问日期」字段，此状态的地点可以关联文章。
   - **想去**：会出现「想去理由」字段。
   - **计划**：会出现「计划日期」字段。
4. 可选填写：
   - **国别（ISO 代码）**：如 `CN`、`US`，多国逗号分隔。驱动国旗显示与国家高亮；
   - **到访年份**：如 `2015,2016`，支持多次到访，驱动年度统计面板与弹窗年份胶囊；
   - **封面图**：从媒体库选择，留空时前台自动用关联文章的特色图；
   - **分类**：自定义分类标识（如 `whc`）。
5. 可选填写关联文章和地点描述。
6. 点击「添加坐标」。

保存成功后地点会出现在左侧列表中（国别列显示国旗与 ISO 码）。

文章编辑页的 Meta Box 也支持**按名称搜索地点**：输入地名（如「厦门」），从候选中选择即可自动回填名称与坐标。

## 在页面中显示地图

新建或编辑一个页面，插入短代码：

```
[travel_map]
```

发布后访问该页面即可看到地图。

### 常用参数

| 参数 | 默认值 | 示例 |
|------|--------|------|
| `width` | `100%` | `width="800px"` |
| `height` | `550px` | `height="600px"` |
| `zoom` | 设置页的值 | `zoom="6"` |
| `center` | 设置页的值 | `center="39.9,116.4"` |
| `filter_tabs` | 设置页的值 | `filter_tabs="false"` |
| `status` | `all` | `status="done"` |
| `filters` | `all,done,wish,plan` | `filters="all,done"` |
| `auto_zoom` | 设置页的值 | `auto_zoom="false"` |

组合示例：

```
[travel_map height="700px" zoom="3"]
[travel_map status="done" filter_tabs="false"]
[travel_map center="48.8566,2.3522" zoom="6" height="600px"]
```

### 其他短代码

- `[travel_map_countries]` — 去过国家国旗墙（国旗 + ISO 码 + 地点数）；
- `[travel_map_markers status="done" type=""]` — 标记归档列表（国旗 + 地名 + 坐标 + 年份 + 游记封面网格）。

## 从 Google Maps 等导入数据（WGS-84 纠偏）

从 Google Maps、KML 等国际地图工具导出的坐标是 WGS-84 坐标系，与高德的 GCJ-02 存在境内约百米级偏移。导入时在弹窗中勾选 **「WGS-84 坐标纠偏」**（默认关闭）：

- 境内坐标（经 73-136 / 纬 18-54）会在浏览器端经高德 `convertFrom` 批量转换为 GCJ-02（单批 40 个，自动分批）；
- 海外坐标保持不变；
- 由高德地图选点或本插件导出的数据**不要**勾选（本身已是 GCJ-02）。

## 从 1.0.x 升级到 1.1.0

升级后首次访问后台或前台地图页面时，插件自动执行数据库迁移（幂等，可安全重复触发）：

1. 状态枚举 `visited/want_to_go/planned` → `done/wish/plan`（存量数据自动转换，REST/AJAX 的旧入参值在 1.1.x 仍被接受并自动映射）；
2. 新增 `country`（国别）、`years`（到访年份）、`cover_image`（封面图）、`type`（分类）四列；
3. 移除三色设置项（标记颜色改由状态决定）。

建议升级前用「坐标管理 → 导出」做一次全量备份。

### 在主题模板中使用

短代码写在文章或页面正文中时，插件自动加载资源。写在主题模板或小工具里则需要显式加载：

```php
<?php
if (function_exists('travel_map_enqueue_assets')) {
    travel_map_enqueue_assets();
}
?>
<?php echo do_shortcode('[travel_map]'); ?>
```

## 故障排除

### 显示「请先配置高德地图API密钥」

设置页的 API 密钥字段为空。这是插件在短代码渲染阶段就返回的提示，与网络无关。

### 一直显示加载动画，地图不出来

按以下顺序排查：

1. **安全密钥是否填了**：只填 Key 不填安全密钥，新申请的 Key 会静默失败。
2. **域名白名单**：高德控制台中该 Key 绑定的域名是否包含当前访问域名（注意 www 与非 www 的区别）。
3. **Key 类型**：必须是「Web 端（JS API）」。
4. **浏览器控制台**：查看是否有来自 `webapi.amap.com` 的请求失败或配额相关的错误信息。
5. **配额**：高德个人开发者有每日调用上限，超限后当天无法加载。

超时约 12 秒后，插件会把加载动画替换为带排查建议的错误提示和「刷新页面」按钮。

### 地图区域高度为 0，或样式完全错乱

主题没有正确调用 `wp_head()` 或 `wp_footer()`。插件内置三层兜底：主样式内联了一份关键 CSS、短代码渲染时会强制输出资源、前端脚本运行时检测到样式缺失会再补注入一次。多数情况可自动恢复。

如果仍有问题，在模板中输出短代码前调用 `travel_map_enqueue_assets()`。

### 地图出来了但没有标记

- 确认坐标管理页里确实有数据。
- 如果短代码带了 `status` 参数，检查该状态下是否有地点。
- 打开浏览器网络面板，查看 `admin-ajax.php` 的 `travel_map_get_markers` 请求：返回 401/403/405 时插件会自动改用 REST 接口 `/wp-json/travel-map/v1/markers` 重试，若两者都失败则通常是安全插件或 WAF 拦截，需要把这两个路径加入白名单。
- 标记数据有 5 分钟缓存，刚添加的地点如果没出现可稍等或在后台重新保存一次。

### 弹窗里看不到关联文章

- 文章必须是**已发布**状态，草稿和私密文章不会显示。
- 地点状态必须是「已去」，另两种状态显示的是简洁弹窗，不查询文章。
- 关联关系需要通过文章 Meta Box 勾选，或在地点编辑表单里选择「关联文章」。

### 主题的图片灯箱劫持了地图里的图片

插件已经在地图容器内阻断了常见灯箱库（fancybox、lightbox、photoswipe 等）的点击事件。如果你的主题使用了其他灯箱方案仍被触发，需要在主题侧把 `.travel-map-container` 排除在灯箱选择器之外。

### 移动端显示异常

- 宽度用 `100%`，不要写固定像素值。
- 插件在窄屏下会自动改为竖版比例并禁用滚轮缩放，无需额外配置。
- 如果高度仍不正常，检查主题是否对 `.travel-map-container` 或其父容器设置了 `overflow` / 固定高度。

### 地点很多时页面卡顿

1.1.0 已启用点聚合（`AMap.MarkerCluster`），密集区域会自动合并为数字气泡，聚合半径与数字上限可在地图设置中调整。若仍感卡顿，可用 `status` 参数把地图拆成几张，或减少「已去」状态的地点数量（这类地点需要额外查询特色图）。

## 卸载

### 停用

后台 → 插件 → 停用。数据表和地点数据都保留，重新启用后数据完好。

### 删除

在插件列表中点击「删除」时，插件会自动清理：

- 删除 `travel_map_markers` 和 `travel_map_post_markers` 两张表，**所有地点数据将永久丢失**。
- 删除 API 密钥、安全密钥、默认缩放、默认中心点、筛选标签开关，以及 1.1.0 新增的全部地图行为设置项（聚合半径与上限、自适应缩放、国家高亮、年度/状态统计面板、缺省筛选状态、缺省封面图、缩放级别上下限）。

删除前请先用坐标管理页的「导出」功能备份数据。

安全密钥与旧版遗留的三个颜色配置项现在也会随卸载一并清除（见 `travel-map.php` 的 `uninstall()`）。唯一未清理的是数据缓存 transient（`travel_map_markers_geojson`），它不含敏感信息，最长 5 分钟后自然过期。

## 相关文档

- [README.md](README.md) — 功能概览与短代码参考
- [USER_MANUAL.md](USER_MANUAL.md) — 完整功能说明与已知限制
- [TECHNICAL_DOCS.md](TECHNICAL_DOCS.md) — 实现结构、数据表、接口清单
